data/raw/ — immutable original data (keep as single source-of-truth)
data/processed/ — derived, intermediate files
data/README.md — data description and provenance
Notes:

Use Git LFS for medium-size files or DVC for large datasets and pipelines.
Do not commit sensitive data (PHI) to public repositories. EOT
